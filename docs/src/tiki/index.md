# Tiki

What Tiki adds on top of the array framework, in the order the pieces depend
on each other. The compiler, scan, and runtime pages are the experiment
directories' own documents.

```{toctree}
:maxdepth: 1

vision
../usage/layouts
../examples/layouts
compile/README
scan/README
runtime/README
```

```{toctree}
:hidden:

compile/COOPERATIVE_PROOF
compile/EXEMPLAR_AUDIT
compile/GH200_PROOF
runtime/ARCHITECTURE
runtime/DECISION-2026-09-05
runtime/VALIDATION-2026-09-06
runtime/repros/README
```
