# @generated by Hollywood. Do not edit by hand.

name: Build and Test
on:
  workflow_dispatch: {}
  pull_request: {}
  merge_group:
    types:
      - checks_requested
  push:
    branches:
      - main
      - test/*
permissions:
  contents: read
concurrency:
  group: ${{ format('{0}-{1}', github.workflow, github.ref) }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}
jobs:
  affected:
    name: Affected paths
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      native: ${{ steps.paths.outputs.native }}
      lint: ${{ steps.paths.outputs.lint }}
      dependencies: ${{ steps.paths.outputs.dependencies }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
          fetch-depth: 0
      - id: paths
        name: Affected paths
        uses: ./.github/actions/ci/affected
        with:
          event: ${{ github.event_name }}
          base: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          head: ${{ github.event.pull_request.head.sha || github.event.merge_group.head_sha || github.sha }}
  workflows:
    name: Generated workflows
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: "24"
      - name: Install workflow dependencies
        run: npm ci --ignore-scripts
        shell: bash
      - name: Check typed workflows
        run: npm run ci check
        shell: bash
  dependencies:
    name: Dependency review
    needs: affected
    if: ${{ needs.affected.outputs.dependencies == 'true' && (github.event_name == 'pull_request' || github.event_name == 'merge_group') }}
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294
        with:
          fail-on-severity: high
          base-ref: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha }}
          head-ref: ${{ github.event.pull_request.head.sha || github.event.merge_group.head_sha }}
  lint:
    name: Lint
    needs: affected
    if: ${{ needs.affected.outputs.lint == 'true' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: pre-commit/action@v3.0.1
  asan:
    name: Linux Sanitizer Tests (ASAN)
    needs:
      - affected
      - workflows
    if: ${{ needs.affected.outputs.native == 'true' }}
    runs-on: ubuntu-22.04-arm
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Native tests
        uses: ./.github/actions/ci/native
        with:
          mode: ASAN
  ubsan:
    name: Linux Sanitizer Tests (UBSAN)
    needs:
      - affected
      - workflows
    if: ${{ needs.affected.outputs.native == 'true' }}
    runs-on: ubuntu-22.04-arm
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Native tests
        uses: ./.github/actions/ci/native
        with:
          mode: UBSAN
  fedora_x86:
    name: Linux Fedora (x86_64)
    needs:
      - affected
      - workflows
    if: ${{ needs.affected.outputs.native == 'true' }}
    runs-on: blacksmith-4vcpu-ubuntu-2404
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Native tests
        uses: ./.github/actions/ci/native
        with:
          mode: fedora
  fedora_arm:
    name: Linux Fedora (aarch64)
    needs:
      - affected
      - workflows
    if: ${{ needs.affected.outputs.native == 'true' }}
    runs-on: ubuntu-22.04-arm
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Native tests
        uses: ./.github/actions/ci/native
        with:
          mode: fedora
  build_and_test:
    name: ${{ matrix.os }} (${{ matrix.toolkit }}, ${{ matrix.arch }})
    if: github.repository == 'ml-explore/mlx'
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        os:
          - Linux
          - Windows
        arch:
          - x86_64
          - aarch64
        toolkit:
          - cpu
          - cuda-12.6
          - cuda-12.9
          - cuda-13.0
        exclude:
          - os: Windows
            arch: aarch64
            toolkit: cuda-12.6
          - os: Windows
            arch: aarch64
            toolkit: cuda-12.9
          - os: Windows
            arch: aarch64
            toolkit: cuda-13.0
          - os: Windows
            arch: x86_64
            toolkit: cuda-12.6
    runs-on: |-
      ${{ case(matrix.os == 'Windows',
               case(matrix.arch == 'aarch64', 'windows-11-arm',
                    startsWith(matrix.toolkit, 'cuda'), 'windows-2022-large',
                    'windows-2022'),
               case(matrix.arch == 'x86_64' && startsWith(matrix.toolkit, 'cuda'), 'gpu-t4-4-core',
                    matrix.arch == 'aarch64', 'ubuntu-22.04-arm',
                    'ubuntu-22.04'))
      }}
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup
        id: setup
        with:
          toolkit: ${{ matrix.toolkit }}
      - uses: ./.github/actions/build
        with:
          cmake-args: ${{ steps.setup.outputs.cmake-args }}
          debug: ${{ matrix.os != 'Windows' }}
      - name: Check generated Python stubs with ty
        if: matrix.os == 'Linux' && matrix.arch == 'x86_64' && matrix.toolkit == 'cpu'
        run: uvx ty check python/mlx/core
        shell: bash
      - uses: ./.github/actions/test-linux
        if: matrix.os == 'Linux' && (matrix.toolkit == 'cpu' || matrix.arch == 'x86_64')
      - uses: ./.github/actions/test-windows
        if: matrix.os == 'Windows' && matrix.toolkit == 'cpu'
  mac_build:
    name: macOS (${{ matrix.macos-target }}, ${{ matrix.toolkit }})
    if: github.repository == 'ml-explore/mlx'
    strategy:
      fail-fast: false
      matrix:
        macos-target:
          - "14.0"
          - "15.0"
          - "26.2"
        toolkit:
          - cpu
          - metal
          - jit
    runs-on: macos-26-xlarge
    needs: lint
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup
        id: setup
        with:
          toolkit: ${{ matrix.toolkit }}
          ccache-key: test-${{ matrix.macos-target }}
          ccache-save: ${{ matrix.toolkit == 'metal' }}
          ccache-toolkit: metal
      - uses: ./.github/actions/build-macos
        with:
          cmake-args: ${{ steps.setup.outputs.cmake-args }}
          macos-target: ${{ matrix.macos-target }}
      - uses: ./.github/actions/test-macos
        if: matrix.toolkit == 'cpu'
        with:
          toolkit: cpu
      - uses: actions/upload-artifact@v7
        if: matrix.toolkit != 'cpu'
        with:
          name: mlx-${{ matrix.toolkit }}-macos${{ matrix.macos-target }}
          path: |
            dist/mlx-*.whl
            build/mlx/backend/metal/kernels/mlx.metallib
            build/tests/tests
          if-no-files-found: error
  mac_test:
    name: Test macOS (${{ matrix.toolkit }})
    if: github.repository == 'ml-explore/mlx'
    runs-on:
      - self-hosted
      - macos
    needs: mac_build
    strategy:
      fail-fast: false
      matrix:
        toolkit:
          - metal
          - jit
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup
        with:
          toolkit: ${{ matrix.toolkit }}
          use-ccache: false
      - uses: actions/download-artifact@v8
        with:
          path: artifact
          pattern: mlx-${{ matrix.toolkit }}-*
      - run: ls -lhR artifact
        shell: bash
      - uses: ./.github/actions/test-macos
        with:
          toolkit: ${{ matrix.toolkit }}
  build_documentation:
    name: Build Documentation
    if: github.repository == 'ml-explore/mlx'
    runs-on: ubuntu-22.04
    needs: lint
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/build-docs
  check_lint:
    name: Check Lint
    if: ${{ always() }}
    needs:
      - affected
      - workflows
      - dependencies
      - lint
      - asan
      - ubsan
      - fedora_x86
      - fedora_arm
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions: {}
    steps:
      - name: Accept required checks
        if: ${{ needs.affected.result == 'success' && needs.workflows.result == 'success' && (needs.affected.outputs.lint == 'true' && needs.lint.result == 'success' || needs.affected.outputs.lint == 'false' && needs.lint.result == 'skipped') && (needs.dependencies.result == 'success' || needs.dependencies.result == 'skipped' && (needs.affected.outputs.dependencies == 'false' || github.event_name == 'push' || github.event_name == 'workflow_dispatch')) && (needs.affected.outputs.native == 'true' && needs.asan.result == 'success' || needs.affected.outputs.native == 'false' && needs.asan.result == 'skipped') && (needs.affected.outputs.native == 'true' && needs.ubsan.result == 'success' || needs.affected.outputs.native == 'false' && needs.ubsan.result == 'skipped') && (needs.affected.outputs.native == 'true' && needs.fedora_x86.result == 'success' || needs.affected.outputs.native == 'false' && needs.fedora_x86.result == 'skipped') && (needs.affected.outputs.native == 'true' && needs.fedora_arm.result == 'success' || needs.affected.outputs.native == 'false' && needs.fedora_arm.result == 'skipped') }}
        run: "true"
        shell: bash
      - name: Reject failed or missing checks
        if: ${{ !(needs.affected.result == 'success' && needs.workflows.result == 'success' && (needs.affected.outputs.lint == 'true' && needs.lint.result == 'success' || needs.affected.outputs.lint == 'false' && needs.lint.result == 'skipped') && (needs.dependencies.result == 'success' || needs.dependencies.result == 'skipped' && (needs.affected.outputs.dependencies == 'false' || github.event_name == 'push' || github.event_name == 'workflow_dispatch')) && (needs.affected.outputs.native == 'true' && needs.asan.result == 'success' || needs.affected.outputs.native == 'false' && needs.asan.result == 'skipped') && (needs.affected.outputs.native == 'true' && needs.ubsan.result == 'success' || needs.affected.outputs.native == 'false' && needs.ubsan.result == 'skipped') && (needs.affected.outputs.native == 'true' && needs.fedora_x86.result == 'success' || needs.affected.outputs.native == 'false' && needs.fedora_x86.result == 'skipped') && (needs.affected.outputs.native == 'true' && needs.fedora_arm.result == 'success' || needs.affected.outputs.native == 'false' && needs.fedora_arm.result == 'skipped')) }}
        run: "false"
        shell: bash
