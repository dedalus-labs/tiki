# @generated by Hollywood. Do not edit by hand.

name: Docs
on:
  push:
    branches:
      - main
    paths:
      - docs/**
      - python/**
      - experiments/**/*.md
      - README.md
      - gha/docs.ts
  pull_request:
    branches:
      - main
    paths:
      - docs/**
      - python/**
      - experiments/**/*.md
      - README.md
      - gha/docs.ts
  workflow_dispatch: {}
concurrency:
  group: pages
  cancel-in-progress: false
permissions:
  contents: read
jobs:
  build:
    name: Build
    if: ${{ github.repository == 'dedalus-labs/tiki' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.13"
      - name: Refresh the package index
        run: sudo apt-get update
        shell: bash
      - name: Install Doxygen and BLAS
        run: sudo apt-get install -y --no-install-recommends doxygen libblas-dev liblapack-dev liblapacke-dev
        shell: bash
      - name: Install Tiki
        env:
          CMAKE_BUILD_PARALLEL_LEVEL: "4"
        run: python -m pip install .
        shell: bash
      - name: Install docs dependencies
        run: python -m pip install -r docs/requirements.txt
        shell: bash
      - name: Index the C++ headers
        working-directory: docs
        run: doxygen
        shell: bash
      - name: Build the site
        working-directory: docs
        run: python -m sphinx -W --keep-going -b html src build/html
        shell: bash
      - name: Upload pages artifact
        uses: actions/upload-pages-artifact@7b1f4a764d45c48632c6b24a0339c27f5614fb0b
        with:
          path: docs/build/html
  deploy:
    name: Deploy
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        name: Deploy to GitHub Pages
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128
